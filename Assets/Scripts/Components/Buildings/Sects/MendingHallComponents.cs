// ECS components for the MendingHall (Renewal sect building).
// Global namespace per project convention.

using Unity.Entities;

/// <summary>Marker for the Mending Hall (Sect of Renewal). Counted by the per-faction build cap of 5.</summary>
public struct MendingHallTag : IComponentData { }
